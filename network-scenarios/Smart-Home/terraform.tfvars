# Variables that are dependendent on the user are declared here

project_id = "tfg-daerv"

proxy_config_path = "../../provisioning-files/proxy-config.tftpl"

proxy_provisioning_path = "../../provisioning-files/docker-proxy-provisioning.tftpl"
